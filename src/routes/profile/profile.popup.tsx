import React from "react";
import styled from "styled-components";

interface IProps {}

const Styled = styled.div``;

const ProfilePopup = (props: IProps) => {
  return <Styled className="profile-popup"></Styled>;
};

export default ProfilePopup;
